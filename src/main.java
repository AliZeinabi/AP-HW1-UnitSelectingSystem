import ConnectToUser.cli;
import courses.*;
import ConnectToUser.cli;
public class main {
    public static void main(String[] args) {
        CourseList courseList = new CourseList();
        cli cli = new cli();
        cli.start();
    }
}